create table employees (
                    id bigint auto_increment,
                    emp_name varchar(255),
                    year_of_birth bigint,
                    primary key (id)
                );